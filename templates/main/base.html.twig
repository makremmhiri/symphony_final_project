{# {% extends 'base.html.twig' %}

{% block body %}
    <!-- Navigation -->
    {{ include('components/Navbar.html.twig') }}
    
    <!-- Main Content Area -->
    <main class="min-vh-100">
        {% block content %}{% endblock %}
    </main>
    
    <!-- Footer -->
    <footer class="bg-dark text-white py-4 mt-auto">
        <div class="container">
            <div class="row">
                <div class="col-md-4 mb-3">
                    <h5 class="fw-bold mb-3">ShopPlus</h5>
                    <p class="text-light mb-0">
                        Gestion des magasins et produits.
                    </p>
                </div>
                <div class="col-md-4 mb-3">
                    <h5 class="fw-bold mb-3">Navigation</h5>
                    <ul class="list-unstyled">
                        <li><a href="{{ path('app_main') }}" class="text-white text-decoration-none">Accueil</a></li>
                        <li><a href="{{ path('magasin_index') }}" class="text-white text-decoration-none">Magasins</a></li>
                        <li><a href="{{ path('produit_index') }}" class="text-white text-decoration-none">Produits</a></li>
                    </ul>
                </div>
                <div class="col-md-4 mb-3">
                    <h5 class="fw-bold mb-3">Support</h5>
                    <ul class="list-unstyled">
                        <li><i class="bi bi-envelope me-2"></i>support@shopplus.com</li>
                        <li><i class="bi bi-telephone me-2"></i>+33 1 23 45 67 89</li>
                    </ul>
                </div>
            </div>
            <hr class="bg-light my-3">
            <div class="text-center">
                <p class="mb-0">&copy; {{ "now"|date("Y") }} ShopPlus. Tous droits réservés.</p>
            </div>
        </div>
    </footer>
{% endblock %} #}