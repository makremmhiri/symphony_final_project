<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{% block title %}ShopPlus{% endblock %}</title>
    
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
    
    {% block stylesheets %}{% endblock %}
</head>
<body>
    <!-- Navigation -->
    <nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm sticky-top">
        <div class="container">
            <!-- Logo à gauche -->
            <a class="navbar-brand fw-bold text-primary" href="{{ path('app_main') }}">
                <i class="bi bi-shop me-2"></i>ShopPlus
            </a>
            
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            
            <div class="collapse navbar-collapse" id="navbarNav">
                <!-- Liens au centre -->
                <ul class="navbar-nav mx-auto">
                    <li class="nav-item">
                        <a class="nav-link {{ app.request.get('_route') == 'app_main' ? 'active' : '' }}" 
                           href="{{ path('app_main') }}">
                            <i class="bi bi-house-door me-1"></i>Accueil
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link {{ app.request.get('_route') in ['app_produit', 'produit_index'] ? 'active' : '' }}" 
                           href="{{ path('app_produit') }}">
                            <i class="bi bi-box me-1"></i>Produits
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link {{ app.request.get('_route') in ['app_magasin', 'magasin_index'] ? 'active' : '' }}" 
                           href="{{ path('app_magasin') }}">
                            <i class="bi bi-shop me-1"></i>Magasins
                        </a>
                    </li>
                    {% if app.user and is_granted('ROLE_ADMIN') %}
                        <li class="nav-item">
                            <a class="nav-link {{ app.request.get('_route') == 'app_user' ? 'active' : '' }}" 
                               href="{{ path('app_user') }}">
                                <i class="bi bi-people me-1"></i>Utilisateurs
                            </a>
                        </li>
                    {% endif %}
                </ul>
                
                <!-- Login à droite -->
                <div class="navbar-nav ms-auto">
                    {% if app.user %}
                        <!-- Si l'utilisateur est connecté -->
                        <div class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle d-flex align-items-center" href="#" role="button" 
                               data-bs-toggle="dropdown">
                                <i class="bi bi-person-circle me-2"></i>
                                {{ app.user.email }}
                            </a>
                            <ul class="dropdown-menu dropdown-menu-end">
                                <li>
                                    <a class="dropdown-item" href="{{ path('app_user_show', {'id': app.user.id}) }}">
                                        <i class="bi bi-person me-2"></i>Mon Profil
                                    </a>
                                </li>
                                <li>
                                    <a class="dropdown-item" href="{{ path('app_user_edit', {'id': app.user.id}) }}">
                                        <i class="bi bi-pencil me-2"></i>Modifier mon compte
                                    </a>
                                </li>
                                {% if is_granted('ROLE_ADMIN') %}
                                    <li><hr class="dropdown-divider"></li>
                                    <li>
                                        <a class="dropdown-item" href="{{ path('app_user') }}">
                                            <i class="bi bi-people me-2"></i>Gestion des utilisateurs
                                        </a>
                                    </li>
                                {% endif %}
                                <li><hr class="dropdown-divider"></li>
                                <li>
                                    <form method="post" action="{{ path('app_logout') }}" class="d-inline">
                                        <button type="submit" class="dropdown-item text-danger">
                                            <i class="bi bi-box-arrow-right me-2"></i>Déconnexion
                                        </button>
                                    </form>
                                </li>
                            </ul>
                        </div>
                    {% else %}
                        <!-- Si l'utilisateur n'est pas connecté -->
                        <a class="btn btn-outline-primary me-2" href="{{ path('app_login') }}">
                            <i class="bi bi-box-arrow-in-right me-1"></i>Connexion
                        </a>
                        <a class="btn btn-primary" href="{{ path('app_user_new') }}">
                            <i class="bi bi-person-plus me-1"></i>Inscription
                        </a>
                    {% endif %}
                </div>
            </div>
        </div>
    </nav>

    <!-- Main Content -->
    <main>
        {% block body %}{% endblock %}
    </main>

    <!-- Footer -->
    <footer class="bg-dark text-white py-4 mt-5">
        <div class="container">
            <div class="row">
                <div class="col-md-4 mb-3">
                    <h5 class="fw-bold mb-3">ShopPlus</h5>
                    <p class="text-light mb-0">
                        Votre plateforme de shopping préférée. Produits de qualité, service client exceptionnel.
                    </p>
                </div>
                <div class="col-md-4 mb-3">
                    <h5 class="fw-bold mb-3">Liens rapides</h5>
                    <ul class="list-unstyled">
                        <li><a href="{{ path('app_main') }}" class="text-white text-decoration-none">Accueil</a></li>
                        <li><a href="{{ path('app_produit') }}" class="text-white text-decoration-none">Produits</a></li>
                        <li><a href="{{ path('app_magasin') }}" class="text-white text-decoration-none">Magasins</a></li>
                    </ul>
                </div>
                <div class="col-md-4 mb-3">
                    <h5 class="fw-bold mb-3">Contact</h5>
                    <ul class="list-unstyled">
                        <li><i class="bi bi-envelope me-2"></i>contact@shopplus.com</li>
                        <li><i class="bi bi-telephone me-2"></i>01 23 45 67 89</li>
                        <li><i class="bi bi-geo-alt me-2"></i>Paris, France</li>
                    </ul>
                </div>
            </div>
            <hr class="bg-light my-4">
            <div class="row">
                <div class="col-md-6">
                    <p class="mb-0">&copy; {{ "now"|date("Y") }} ShopPlus. Tous droits réservés.</p>
                </div>
                <div class="col-md-6 text-md-end">
                    <a href="#" class="text-white text-decoration-none me-3">Mentions légales</a>
                    <a href="#" class="text-white text-decoration-none me-3">Politique de confidentialité</a>
                    <a href="#" class="text-white text-decoration-none">Contact</a>
                </div>
            </div>
        </div>
    </footer>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    
    {% block javascripts %}{% endblock %}
</body>
</html>